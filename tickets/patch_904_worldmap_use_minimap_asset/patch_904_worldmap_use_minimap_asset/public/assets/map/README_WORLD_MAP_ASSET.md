# World map asset

Place ici l’image finale utilisée par le jeu :

```text
public/assets/map/world_map.png
```

Le patch doit utiliser cet asset en priorité.

Si l’image existe déjà dans le repo ou dans `design_boards`, Claude Code doit la copier ici sans supprimer la source.
