# OpenMoji Local Clone Notes

User clone path:

```txt
snake/tmp/emoticon
```

Expected exported SVG folder:

```txt
snake/tmp/emoticon/color/svg/
```

Metadata may exist at:

```txt
snake/tmp/emoticon/data/openmoji.json
```

Use `color/svg`.

Commit only the selected copied SVGs, never the full clone.
