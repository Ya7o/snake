/*
Interface attendue pour src/systems/DesignBoardManager.ts

export interface UniverseDesignAssets {
  boardSource: string;
  assetBase: string;
  confidence: string;
  assets: Record<string, string>;
  fallbacks: string[];
}

export class DesignBoardManager {
  async load(): Promise<void> {}
  getUniverseAssets(universeId: string): UniverseDesignAssets | undefined {}
  hasRealAsset(universeId: string, key: string): boolean { return false; }
}
*/
